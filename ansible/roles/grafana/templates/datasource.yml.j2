apiVersion: 1

datasources:
  - name: Prometheus
    uid: de8hb37uebgu8b
    type: prometheus
    access: proxy
    url: http://localhost:9090
    editable: false
    basicAuth: true
    basicAuthUser: grafana
    secureJsonData:
      basicAuthPassword: "{{ grafana_prometheus_password }}"
